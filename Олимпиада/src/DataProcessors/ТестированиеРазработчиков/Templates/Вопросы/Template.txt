<?xml version="1.0" encoding="UTF-8"?>
<Вопросы>
	<Вопрос Код="1">
		<Тема>Сравнение структур</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	Результат = ЗадачаНаСервере();
	
	Сообщить(Результат);
	
КонецПроцедуры

&amp;НаСервере
Функция ЗадачаНаСервере()
	
	Стр1 = Новый Структура;
	Стр2 = Новый Структура;
	
	Возврат Стр1 = Стр2;
	
КонецФункции</ТекстВопроса>
		<Ответы>!Нет
Да
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="2">
		<Тема>Сравнение массивов</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	Результат = ЗадачаНаСервере();
	
	Сообщить(Результат);
	
КонецПроцедуры

&amp;НаСервере
Функция ЗадачаНаСервере()
	
	Мас1 = Новый Массив(1);
	Мас1[0] = 5;
	
	Мас2 = Новый Массив;
	Мас2[0] = 5;
	
	Возврат Мас1 = Мас2;
	
КонецФункции</ТекстВопроса>
		<Ответы>!Нет
Да
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="3">
		<Тема>Сравнение массивов</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	Результат = ЗадачаНаСервере();
	
	Сообщить(Результат);
	
КонецПроцедуры

&amp;НаСервере
Функция ЗадачаНаСервере()
	
	Мас1 = Новый Массив(0);
	Мас2 = Новый Массив(0);
	
	Возврат Мас1 = Мас2;
	
КонецФункции</ТекстВопроса>
		<Ответы>Нет
Да
!#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="4">
		<Тема>Сравнение незаполненных значений</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	РезультатАлгоритма = ЗадачаНаСервере();
	
	Сообщить(РезультатАлгоритма);
	
КонецПроцедуры

&amp;НаСервере
Функция ЗадачаНаСервере()

	Возврат (Null = Неопределено);
	
КонецФункции</ТекстВопроса>
		<Ответы>!Нет
Да
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="5">
		<Тема>Условие ИЛИ</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	Счетчик = 0;
	
	ЗадачаНаСервере(Счетчик);
	
	Сообщить(Счетчик);
	
КонецПроцедуры

&amp;НаСервере
Функция ЗадачаНаСервере()
	
	Возврат Сравнение1(Счетчик)
		ИЛИ Сравнение2(Счетчик)
		ИЛИ Сравнение3(Счетчик)
		ИЛИ Сравнение4(Счетчик)
		ИЛИ Сравнение5(Счетчик);
	
КонецФункции

Функция Сравнение1()
	
	Счетчик = Счетчик + 1;
	Возврат Ложь;
	
КонецФункции

Функция Сравнение2()
	
	Счетчик = Счетчик + 1;
	Возврат Ложь;
	
КонецФункции

Функция Сравнение3()
	
	Счетчик = Счетчик + 1;
	Возврат Истина;
	
КонецФункции

Функция Сравнение4()
	
	Счетчик = Счетчик + 1;
	Возврат Ложь;
	
КонецФункции

Функция Сравнение5()
	
	Счетчик = Счетчик + 1;
	Возврат Истина;
	
КонецФункции</ТекстВопроса>
		<Ответы>0
1
2
!3
4
5
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="6">
		<Тема>Условие И</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	Счетчик = 0;
	
	ЗадачаНаСервере(Счетчик);
	
	Сообщить(Счетчик);
	
КонецПроцедуры

&amp;НаСервере
Функция ЗадачаНаСервере()
	
	Возврат Сравнение1(Счетчик)
		  И Сравнение2(Счетчик)
		  И Сравнение3(Счетчик)
		  И Сравнение4(Счетчик)
		  И Сравнение5(Счетчик);
	
КонецФункции

Функция Сравнение1()
	
	Счетчик = Счетчик + 1;
	Возврат Ложь;
	
КонецФункции

Функция Сравнение2()
	
	Счетчик = Счетчик + 1;
	Возврат Ложь;
	
КонецФункции

Функция Сравнение3()
	
	Счетчик = Счетчик + 1;
	Возврат Истина;
	
КонецФункции

Функция Сравнение4()
	
	Счетчик = Счетчик + 1;
	Возврат Ложь;
	
КонецФункции

Функция Сравнение5()
	
	Счетчик = Счетчик + 1;
	Возврат Истина;
	
КонецФункции</ТекстВопроса>
		<Ответы>0
!1
2
3
4
5
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="7">
		<Тема>Передача параметров</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	СтруктураДанных = Новый Структура;
	СтруктураДанных.Вставить("Значение", 1);
	
	ЗадачаНаСервере(СтруктураДанных);
	
	Сообщить(СтруктураДанных.Значение);
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере(СтруктураДанных)
	
	СтруктураДанных.Значение = 100;	
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>1
!100
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="8">
		<Тема>Передача параметров</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	СтруктураДанных = Новый Структура;
	СтруктураДанных.Вставить("Значение", 1);
	
	ФиксированнаяСтруктураДанных = Новый ФиксированнаяСтруктура(СтруктураДанных);
	
	ЗадачаНаСервере(ФиксированнаяСтруктураДанных.Значение);
	
	Сообщить(СтруктураДанных.Значение);
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере(ПереданноеЗначение)
		
	ПереданноеЗначение = 100;
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>1
100
!#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="9">
		<Тема>Передача параметров</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	СтруктураДанных = Новый Структура;
	СтруктураДанных.Вставить("Значение", 1);
	
	ФиксированнаяСтруктураДанных = Новый ФиксированнаяСтруктура(СтруктураДанных);
	
	ЗадачаНаСервере(ФиксированнаяСтруктураДанных);
	
	Сообщить(ФиксированнаяСтруктураДанных.Значение);
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере(ФиксированнаяСтруктураДанных)
	
	СтруктураДанных = Новый Структура;
	СтруктураДанных.Вставить("Значение", 100);
	
	ФиксированнаяСтруктураДанных = Новый ФиксированнаяСтруктура(СтруктураДанных);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>1
!100
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="10">
		<Тема>Сравнение дат</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()
	
	ТекущаяДатаНаСервере = ТекущаяДатаСеанса();
	ИзмененнаяДатаНаСервере = ТекущаяДатаНаСервере + 0.001;
	
	ДатыРавны = (ТекущаяДатаНаСервере = ИзмененнаяДатаНаСервере);
	
	Сообщить(ДатыРавны);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>!Нет
Да
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="11">
		<Тема>Сравнение дат</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()
	
	ТекущаяДатаНаСервере = ТекущаяДатаСеанса();
	ИзмененнаяДатаНаСервере = ТекущаяДатаНаСервере + 0.001;
	
	ДатыРавны = (Строка(ТекущаяДатаНаСервере) = Строка(ИзмененнаяДатаНаСервере));
	
	Сообщить(ДатыРавны);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>Нет
!Да
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="12">
		<Тема>Попытка в транзакции</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()
	
	НачатьТранзакцию();
	
	Попытка
		
		ХранилищеОбщихНастроек.Сохранить("МоеЗначение", "МойКлюч", 100);
		
		ВызватьИсключение "Ты не пройдешь!";
		
		ЗафиксироватьТранзакцию();
		
	Исключение
		
		Если ТранзакцияАктивна() Тогда
			ОтменитьТранзакцию();
		КонецЕсли;
		
	КонецПопытки;
	
	ЗначениеИзХранилища = ХранилищеОбщихНастроек.Загрузить("МоеЗначение", "МойКлюч");
	
	Сообщить(ЗначениеИзХранилища);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>NULL
!Неопределено
100
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="13">
		<Тема>Запутанная попытка в транзакции</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()
	
	ПроверочноеЗначение = Неопределено;
	
	НачатьТранзакцию();
	
	Попытка
		
		ХранилищеОбщихНастроек.Сохранить("МоеЗначение", "МойКлюч", 1);
		
		ВызватьИсключение "Ты не пройдешь!";
		
		ЗафиксироватьТранзакцию();
		
	Исключение
		
		ХранилищеОбщихНастроек.Сохранить("МоеЗначениеПроверочное", "МойКлючПроверочное", 100);
		ПроверочноеЗначение = ХранилищеОбщихНастроек.Загрузить("МоеЗначениеПроверочное", "МойКлючПроверочное");
		
		Если ТранзакцияАктивна() Тогда
			ОтменитьТранзакцию();
		КонецЕсли;
		
	КонецПопытки;
	
	ЗначениеИзХранилища = ХранилищеОбщихНастроек.Загрузить("МоеЗначение", "МойКлюч");
	
	Сообщить(ПроверочноеЗначение);	
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>NULL
Неопределено
1
!100
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="14">
		<Тема>Сравнение чисел</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()
	
	ЧислоПроверка = 0.123456789012345678901234567890123456789;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	&amp;ЧислоПроверка КАК ЧислоПроверка
		|ПОМЕСТИТЬ ВТ
		|;
		|
		|ВЫБРАТЬ
		|	ВТ.ЧислоПроверка КАК ЧислоПроверка
		|ИЗ
		|	ВТ КАК ВТ";	
	Запрос.УстановитьПараметр("ЧислоПроверка", ЧислоПроверка);
	Выборка = Запрос.Выполнить().Выбрать();
	Выборка.Следующий();
	
	ЧислаРавны = Выборка.ЧислоПроверка = ЧислоПроверка;
	Сообщить("ЧислаРавны: " + ЧислаРавны);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>!Нет
Да
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="15">
		<Тема>Сравнение строк</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()

	СтрокиРавны = ("Привет" = "ПРИВЕТ");
	
	Сообщить(СтрокиРавны);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>!Нет
Да
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="16">
		<Тема>Сравнение строк</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()

	СтрокиРавны = ("Привет" = "Привет");
	
	Сообщить(СтрокиРавны);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>Нет
!Да
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="17">
		<Тема>Рекурсия</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	Счетчик = 0;
	
	ЗадачаНаСервере(Счетчик);
	
	Сообщить(Счетчик);
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере(Счетчик = 0)
	
	Счетчик = Счетчик + 1;
	
	ЗадачаНаСервере(Счетчик)
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>0
1
9999
10000
65536
4294967296
!#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="18">
		<Тема>Поиск в таблице значений</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()
	
	ТаблицаДанных = Новый ТаблицаЗначений;
	ТаблицаДанных.Колонки.Добавить("ЗначениеПоиска");
	ТаблицаДанных.Колонки.Добавить("Значение");
	
	Запись = ТаблицаДанных.Добавить();
	Запись.ЗначениеПоиска = "ЗначениеR39;поиска";
	Запись.Значение = 100;
	
	НайденноеЗначение = 0;
	
	ПоискЗаписи = ТаблицаДанных.НайтиСтроки(Новый Структура("ЗначениеПоиска", "Значение поиска"));
	Если ПоискЗаписи.Количество() &gt; 0 Тогда
		
		НайденноеЗначение = ПоискЗаписи.Получить(0).Значение;
		
	Иначе
		
		НайденноеЗначение = 0;
		
	КонецЕсли;
	
	Сообщить(НайденноеЗначение);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>!0
1
100
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="19">
		<Тема>Работа с потоками</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()
	
	МассивЗначений = Новый Массив;
	МассивЗначений.Добавить(0);
	МассивЗначений.Добавить(1);
	МассивЗначений.Добавить(2);
	МассивЗначений.Добавить(3);
	МассивЗначений.Добавить(4);
	МассивЗначений.Добавить(5);
	
	ПотокВПамяти = Новый ПотокВПамяти();
    ЗаписьДанных = Новый ЗаписьДанных(ПотокВПамяти);
	
	Для Каждого ЭлементМассива Из МассивЗначений Цикл
		ЗаписьДанных.ЗаписатьЦелое64(ЭлементМассива);
	КонецЦикла;
	
	ЗаписьДанных.Закрыть();
	
	МассивКакДвоичныеДанные = ПотокВПамяти.ЗакрытьИПолучитьДвоичныеДанные();
	
	ВременныйФайл = ПолучитьИмяВременногоФайла("tmp");
	МассивКакДвоичныеДанные.Записать(ВременныйФайл);
	
	ТекстовыйДокумент = Новый ТекстовыйДокумент;
	ТекстовыйДокумент.Прочитать(ВременныйФайл);
	ВесьТекст = ТекстовыйДокумент.ПолучитьТекст();
	
	Сообщить(ВесьТекст);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>""
"012345"
"000000000000000100000002000000030000000400000005"
!#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="20">
		<Тема>СОМ-соединение</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	WshShell = Новый COMОбъект("WScript.Shell");
	WshShell.Run("notepad");
	
	Счетчик = 0;
	
	ЗадачаНаСервере(WshShell, Счетчик);
	
	Сообщить(Счетчик);
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере(WshShell, Счетчик)
	
	Счетчик = Счетчик + 1;
	
	Попытка
		
		WshShell.Run("notepad");	
	
		Счетчик = Счетчик + 1;
		
	Исключение
	
	КонецПопытки;
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>0
1
2
!#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="21">
		<Тема>СОМ-соединение</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()
	
	WshShell = Новый COMОбъект("WScript.Shell");
	WshShell.Run("notepad");
	
	АдресВХранилище = ПоместитьВоВременноеХранилище(WshShell, УникальныйИдентификатор);
	
	ОбъектИзХранилища = ПолучитьИзВременногоХранилища(АдресВХранилище);
	ТипОбъектаИзХранилища = ТипЗнч(ОбъектИзХранилища);
	
	Сообщить(ТипОбъектаИзХранилища);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>Неопределено
COMОбъект
WScript.Shell
!#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="22">
		<Тема>Запросы</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	1 КАК ЧислоЗначение
		|ПОМЕСТИТЬ ВТ_Значения
		|
		|ОБЪЕДИНИТЬ ВСЕ
		|
		|ВЫБРАТЬ
		|	2
		|
		|ОБЪЕДИНИТЬ ВСЕ
		|
		|ВЫБРАТЬ
		|	3
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ВТ_Значения_1.ЧислоЗначение КАК ЧислоЗначение_1,
		|	ВТ_Значения_2.ЧислоЗначение КАК ЧислоЗначение_2,
		|	ВТ_Значения_3.ЧислоЗначение КАК ЧислоЗначение_3
		|ИЗ
		|	ВТ_Значения КАК ВТ_Значения_1,
		|	ВТ_Значения КАК ВТ_Значения_2,
		|	ВТ_Значения КАК ВТ_Значения_3";
	
	РезультатыЗапроса = Запрос.ВыполнитьПакет();
	
	Выборка1 = РезультатыЗапроса[0].Выбрать();
	Выборка2 = РезультатыЗапроса[1].Выбрать();
	
	ОбщееКоличествоЗаписей = Выборка1.Количество() + Выборка2.Количество();
	
	Сообщить(ОбщееКоличествоЗаписей);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>1
2
3
9
10
27
!28
30
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="23">
		<Тема>Запросы</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()
	
	ТаблицаДляЗапроса = Новый ТаблицаЗначений;
	ТаблицаДляЗапроса.Колонки.Добавить("ЗначениеСтрока", Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(150)));
	ТаблицаДляЗапроса.Колонки.Добавить("ЗначениеЧисло", Новый ОписаниеТипов("Число",,,Новый КвалификаторыЧисла(15,2)));
	
	Запись = ТаблицаДляЗапроса.Добавить();
	Запись.ЗначениеСтрока = "Один";
	Запись.ЗначениеЧисло = 1;
	
	Запись = ТаблицаДляЗапроса.Добавить();
	Запись.ЗначениеСтрока = "Два";
	Запись.ЗначениеЧисло = 2;
	
	Запись = ТаблицаДляЗапроса.Добавить();
	Запись.ЗначениеСтрока = "Три";
	Запись.ЗначениеЧисло = 3;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ
		|	Т1.ЗначениеСтрока КАК ЗначениеСтрока1,
		|	Т1.ЗначениеЧисло КАК ЗначениеЧисло1,
		|	Т2.ЗначениеСтрока КАК ЗначениеСтрока2,
		|	Т2.ЗначениеЧисло КАК ЗначениеЧисло2
		|ПОМЕСТИТЬ ВТ_ДанныеТаблицыЗначений
		|ИЗ
		|	&amp;ТаблицаДляЗапроса КАК Т1,
		|	&amp;ТаблицаДляЗапроса КАК Т2
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	ВТ_ДанныеТаблицыЗначений.ЗначениеСтрока1 КАК ЗначениеСтрока1,
		|	ВТ_ДанныеТаблицыЗначений.ЗначениеЧисло1 КАК ЗначениеЧисло1,
		|	ВТ_ДанныеТаблицыЗначений.ЗначениеСтрока1 КАК ЗначениеСтрока2,
		|	ВТ_ДанныеТаблицыЗначений.ЗначениеЧисло1 КАК ЗначениеЧисло2
		|ИЗ
		|	ВТ_ДанныеТаблицыЗначений КАК ВТ_ДанныеТаблицыЗначений";
	
	РезультатЗапроса = Запрос.Выполнить();
	Выборка = РезультатЗапроса.Выбрать();
	КоличествоЗаписей = Выборка.Количество();
	
	Сообщить(КоличествоЗаписей);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>1
3
6
9
!#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="24">
		<Тема>Запросы</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ ПЕРВЫЕ 0
		|	Т.Значение КАК Значение
		|ИЗ
		|	(ВЫБРАТЬ
		|		1 КАК Значение
		|	
		|	ОБЪЕДИНИТЬ ВСЕ
		|	
		|	ВЫБРАТЬ
		|		2) КАК Т";
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	КоличествоЗаписей = ВыборкаДетальныеЗаписи.Количество();
	
	Сообщить(КоличествоЗаписей);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>!0
1
2
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="25">
		<Тема>Передача параметров</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	СтруктураДанных = Новый Структура;
	СтруктураДанных.Вставить("Значение", 1);
	
	ФиксированнаяСтруктураДанных = Новый ФиксированнаяСтруктура(СтруктураДанных);
	
	ЗадачаНаСервере(ФиксированнаяСтруктураДанных.Значение);
	
	Сообщить(СтруктураДанных.Значение);
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере(Знач ПереданноеЗначение)
	
	ПереданноеЗначение = 100;
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>!1
100
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="26">
		<Тема>Передача параметров</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	МассивЗначений = Новый Массив;
	МассивЗначений.Добавить(1000);
	МассивЗначений.Добавить(Новый Структура("Значение", 2000));
	МассивЗначений.Добавить(Дата(2019, 11, 11));
	
	ЗадачаНаСервере(МассивЗначений, МассивЗначений);
	
	Результат = МассивЗначений.Получить(0)
		+ МассивЗначений.Получить(1).Значение
		+ Год(МассивЗначений.Получить(2));
		
	Сообщить(Результат);
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере(Знач МассивЗначений1, МассивЗначений2)
	
	МассивЗначений1.Установить(0, 10000);
	МассивЗначений1.Получить(1).Значение = 20000;
	МассивЗначений1.Установить(2, Дата(2020,11,11));
	
	МассивЗначений2.Установить(0, 10000);
	МассивЗначений2.Получить(1).Значение = 20000;
	МассивЗначений2.Установить(2, Дата(2020,11,11));
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>5019
!32020
64040
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="27">
		<Тема>Сравнение дат</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()
	
	ДатыРавны = (ТекущаяДата() = ТекущаяДатаСеанса());
	
	Сообщить(ДатыРавны);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>Нет
!Да
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="28">
		<Тема>Вычисления в цикле</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()
	
	РезультатВычислений = 0;
	
	Для Сч = 1 По 10 Цикл
		
		Остаток = Сч % 2;
		Если Остаток &gt; 0 Тогда
			
			РезультатВычислений = РезультатВычислений + Сч;

		КонецЕсли;
		
	КонецЦикла;
	
	Сообщить(РезультатВычислений);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>0
2
!25
30
55
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="29">
		<Тема>Проверка заполнения</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()
	
	ПустойGUID = Новый УникальныйИдентификатор("00000000-0000-0000-0000-000000000000");
	
	GUIDЗаполнен = ЗначениеЗаполнено(ПустойGUID);
	
	Сообщить(GUIDЗаполнен);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>!Нет
Да
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="30">
		<Тема>Вычисления</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()
	
	РезультатВычислений = Pow((5 * 1) / (7 % 2) * Sqrt(9), 2);
	
	ВызватьИсключение Строка(РезультатВычислений);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>225
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="31">
		<Тема>Вычисления</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура Задача(Команда)
	
	ЗадачаНаСервере();
	
КонецПроцедуры

&amp;НаСервере
Процедура ЗадачаНаСервере()
	
	ИсходноеЗначение = 1.5;
	
	НовоеЗначение1 = Окр(ИсходноеЗначение, 2, РежимОкругления.Окр15как20); 
	НовоеЗначение2 = Окр(ИсходноеЗначение, 2, РежимОкругления.Окр15как10);
	
	ЗначенияРавны = (НовоеЗначение1 = НовоеЗначение2);
	
	Сообщить(ЗначенияРавны);
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>Нет
!Да
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="32">
		<Тема>Передача параметров</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура ПередачаПараметровКлиентСервер()

	Список = Новый СписокЗначений;
	Список.Добавить("привет");

	ПередачаПоСсылкеКлиент(Список);

	Сообщить(Список.Количество());

КонецПроцедуры

&amp;НаКлиенте
Процедура ПередачаПоСсылкеКлиент(Параметр)

	Список = Новый СписокЗначений;
	Список.Добавить("привет");
	Список.Добавить("привет");
	
	Параметр = Список;

КонецПроцедуры</ТекстВопроса>
		<Ответы>0
1
!2
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="33">
		<Тема>Передача параметров</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура ПередачаПараметровКлиентСервер()

	Список = Новый СписокЗначений;
	Список.Добавить("привет");

	ПередачаПоЗначениюКлиент(Список);

КонецПроцедуры

&amp;НаКлиенте
Процедура ПередачаПоЗначениюКлиент(Знач Параметр)

	Список = Новый СписокЗначений;
	Список.Добавить("привет");
	Список.Добавить("привет");

	Параметр = Список;

КонецПроцедуры</ТекстВопроса>
		<Ответы>0
!1
2
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="34">
		<Тема>Передача параметров</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура ПередачаПараметровКлиентСервер()

	Список = Новый СписокЗначений;
	Список.Добавить("привет");

	ПередачаПоЗначениюКлиент(Список);

	Сообщить(Список.Количество());

КонецПроцедуры

&amp;НаКлиенте
Процедура ПередачаПоЗначениюКлиент(Знач Параметр)

	Параметр.Добавить("привет");
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>0
1
!2
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="35">
		<Тема>Передача параметров</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура ПередачаПараметровКлиентСервер()

	Список = Новый СписокЗначений;
	Список.Добавить("привет");

	ПередачаПоСсылкеСервер(Список);

	Сообщить(Список.Количество());

КонецПроцедуры

&amp;НаСервере
Процедура ПередачаПоСсылкеСервер(Параметр)

	Параметр.Добавить("привет");
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>0
1
!2
#ОшибкаВыполнения</Ответы>
	</Вопрос>
	<Вопрос Код="36">
		<Тема>Передача параметров</Тема>
		<ТекстВопроса>&amp;НаКлиенте
Процедура ПередачаПараметровКлиентСервер()

	Список = Новый СписокЗначений;
	Список.Добавить("привет");

	ПередачаПоЗначениюСервер(Список);

	Сообщить(Список.Количество());

КонецПроцедуры

&amp;НаСервере
Процедура ПередачаПоЗначениюСервер(Знач Параметр)

	Параметр.Добавить("привет");
	
КонецПроцедуры</ТекстВопроса>
		<Ответы>0
!1
2
#ОшибкаВыполнения</Ответы>
	</Вопрос>
</Вопросы>